import SectionDescription from './_components/SectionDescription/SectionDescription';

const Tours = () => (
  <main>
    <h1 className="visually-hidden">Список туров</h1>
    <SectionDescription />
  </main>
);

export default Tours;
